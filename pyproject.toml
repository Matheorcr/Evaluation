[project]
name = "evaluation-dash"
version = "0.1.0"
description = "Application Dash pour evaluation universitaire"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dash>=4.0.0",
    "dash-bootstrap-components>=2.0.4",
    "pandas>=3.0.1",
    "plotly>=6.6.0",
]
